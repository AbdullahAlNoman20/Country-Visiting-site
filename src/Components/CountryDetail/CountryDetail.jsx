import CountryData from "../CountryData/CountryData";

const CountryDetail = ({country,handelVisitedCountries}) => {
    // console.log(country)
    return (
        <div>
            <h3>Country Details:</h3>

            {/* <CountryData
            country = {country}
            ></CountryData> */}
        </div>
    );
};

export default CountryDetail;